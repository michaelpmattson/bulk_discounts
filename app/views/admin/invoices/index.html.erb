<h2>Invoices</h2>
<ul>
  <% @invoices.each do |invoice| %>
    <li id="invoice-<%= invoice.id %>"><%= link_to "Invoice ##{invoice.id}", admin_invoice_path(invoice.id), method: :get %></li>
  <% end %>
</ul>
